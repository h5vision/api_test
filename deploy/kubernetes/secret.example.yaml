apiVersion: v1
kind: Secret
metadata:
  name: vision-secrets
  namespace: vision
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  QDRANT_API_KEY: "REPLACE_ME"
  AI_PROVIDER_MASTER_KEY: "REPLACE_ME"
