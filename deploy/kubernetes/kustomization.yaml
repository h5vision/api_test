apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - shared-workspace-pvc.yaml
  - api.yaml
  - worker.yaml
  - admin.yaml
  - ingress.yaml
  - pdb.yaml
# migration-job.yaml is intentionally applied by the deployment pipeline before
# rolling out application Deployments.
# keda-worker.yaml requires KEDA and is applied after the worker Deployment.
