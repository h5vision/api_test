# 0724 작업 내용

> 문서 성격: `/v1/IngestResponse` BackendAPI 구현 결과 및 Frontend 팀 인계 문서  
> 작업일: 2026-07-24  
> 작업 범위: BackendAPI · PostgreSQL Project Registry · 관리자 Endpoint 모니터링 · API 문서  
> 범위 제외: VS Code Extension Source 구현

---

## 1. 작업 결과 요약

| 기능·요구사항명 | 이번 작업 결과 | 상태 |
|---|---|---|
| `/v1/IngestResponse` | 프로젝트명과 인덱싱 기준 Git 버전 목록을 반환하는 GET API 구현 | Backend 완료 |
| 인덱싱 버전 비교 (Frontend) | 비교에 필요한 응답 계약과 TypeScript 예시 제공 | Frontend 구현 필요 |
| 인덱싱 버전 비교 (Backend) | PostgreSQL에 저장된 Git SHA와 인덱스 상태 공개 | Backend 완료 |
| 오류 상태 표시 | 빈 목록·DB 장애·통신 장애·인덱스 상태별 처리 규칙 정의 | Backend 완료 · Frontend 구현 필요 |
| 관리자 페이지 | 새 Endpoint의 요청·응답 여부와 응답시간 확인 항목 연결 | 해당 범위 완료 |

Frontend 팀이 관리하는 다음 Extension Source는 이번 작업에서 수정하지 않았다.

```text
C:\Users\PC2412\Documents\HancomAI5\vision-frontend
```

BackendAPI 팀은 API 구현, Swagger/OpenAPI, TypeScript 인계 계약까지만 담당한다.

---

## 2. `/v1/IngestResponse`

### 2.1 사용 목적

VS Code Extension이 다음 시점에 Backend 프로젝트 목록을 조회하기 위한 API다.

1. Extension 최초 활성화
2. Sidebar의 `목록 새로고침` 선택

Backend는 PostgreSQL Project Registry에 등록된 프로젝트명, Git 버전과 인덱스
상태를 반환한다.

### 2.2 요청

```http
GET /v1/IngestResponse
Accept: application/json
```

- Request body를 보내지 않는다.
- 특정 `project_id`를 요청에 넣지 않는다.
- URL 경로는 대소문자를 구분하므로 `/v1/IngestResponse`를 그대로 사용한다.
- Extension 최초 실행과 수동 새로고침에서 같은 호출 함수를 재사용한다.

Production:

```text
https://api.blakeedenparker.cloud/v1/IngestResponse
```

LAN:

```text
http://192.168.0.7:8000/v1/IngestResponse
```

### 2.3 성공 응답

```json
{
  "schema_version": "1.0",
  "request_id": "req_01...",
  "projects": [
    {
      "project_id": "h5vision/protoFastAPI",
      "project_name": "protoFastAPI",
      "git_commit_sha": "4ea031ecb0f8f503e3d8ef27b01e53d771ab1234",
      "git_short_sha": "4ea031e",
      "git_branch": "main",
      "git_dirty": false,
      "git_committed_at": "2026-07-24T03:10:00Z",
      "active_snapshot_id": "snap_01...",
      "index_status": "ready",
      "indexed_at": "2026-07-24T03:15:00Z"
    }
  ],
  "total": 1,
  "generated_at": "2026-07-24T03:20:00Z"
}
```

Git 정보가 없는 프로젝트의 Git 관련 Field는 생략하지 않고 `null`을 반환한다.

### 2.4 Frontend TypeScript 계약

Frontend 팀은 다음과 같은 타입으로 응답을 받을 수 있다.

```ts
export type IndexedProjectStatus =
  | "not_indexed"
  | "queued"
  | "indexing"
  | "ready"
  | "partially_ready"
  | "failed"
  | "stale";

export interface IndexedProjectItem {
  project_id: string;
  project_name: string;
  git_commit_sha: string | null;
  git_short_sha: string | null;
  git_branch: string | null;
  git_dirty: boolean | null;
  git_committed_at: string | null;
  active_snapshot_id: string | null;
  index_status: IndexedProjectStatus;
  indexed_at: string | null;
}

export interface IndexedProjectListResponse {
  schema_version: "1.0";
  request_id: string;
  projects: IndexedProjectItem[];
  total: number;
  generated_at: string;
}
```

호출 예시:

```ts
export async function fetchIndexedProjects(
  baseUrl: string
): Promise<IndexedProjectListResponse> {
  const origin = baseUrl.replace(/\/+$/, "");
  const response = await fetch(`${origin}/v1/IngestResponse`, {
    method: "GET",
    headers: {
      Accept: "application/json"
    },
    cache: "no-store"
  });

  const payload = await response.json();
  if (!response.ok) {
    const code = payload?.error?.code ?? `HTTP_${response.status}`;
    const message =
      payload?.error?.message ?? "프로젝트 목록을 불러올 수 없습니다.";
    throw new Error(`${code}: ${message}`);
  }

  return payload as IndexedProjectListResponse;
}
```

이 TypeScript는 Frontend 팀 구현을 위한 계약 예시다. BackendAPI 팀이 Extension
Source에 직접 추가하지 않는다.

---

## 3. Git SHA 비교의 목적

### 3.1 SHA가 의미하는 것

Git commit SHA는 Repository의 특정 commit을 식별하는 값이다. 이 기능에서는
다음 두 버전의 기준점을 비교하기 위해 사용한다.

```text
Extension이 현재 열어 둔 로컬 Repository의 HEAD commit SHA
                           ↕
Backend가 인덱스를 생성할 때 기준으로 사용한 git_commit_sha
```

목적은 사용자가 현재 보고 있는 코드와 sLLM이 RAG 검색에 사용하는 코드가 같은
commit 기준인지 확인하는 것이다.

### 3.2 비교 결과

```text
local HEAD SHA == backend git_commit_sha
→ 최신
→ 로컬 commit과 Backend 인덱스가 같은 commit을 기준으로 함

local HEAD SHA != backend git_commit_sha
→ 버전 불일치
→ 질문 중인 로컬 코드와 Backend 검색 근거가 서로 다른 commit일 수 있음

Backend에 매칭되는 프로젝트가 없음
→ 인덱싱되지 않음

양쪽 중 하나의 SHA가 null
→ 비교 불가
```

SHA가 다르다는 사실만으로 어느 쪽이 더 최신인지 단정하지 않는다. 최신 방향을
판단하려면 commit ancestry 또는 Git commit 시각을 추가로 확인해야 한다.

### 3.3 전체 SHA와 short SHA

- 실제 비교에는 전체 `git_commit_sha`를 사용한다.
- `git_short_sha`는 Sidebar 표시용이다.
- Branch 이름과 수정 날짜만으로 같은 버전이라고 판정하지 않는다.

### 3.4 로컬 dirty 상태

로컬 파일을 수정했지만 commit하지 않았다면 HEAD SHA는 Backend SHA와 같을 수
있다. 따라서 Frontend는 다음 상태를 분리해서 표시해야 한다.

```text
인덱스 버전 일치 · 로컬 수정사항 있음
```

Backend 응답의 `git_dirty`는 인덱싱 원본 당시 상태다. 현재 사용자 PC의 dirty
상태는 Extension이 VS Code Git API 등을 통해 별도로 확인한다.

---

## 4. Backend 구현 내용

### 4.1 공개 응답 Schema

`backend/schemas.py`에 다음 모델을 추가했다.

```text
IndexedProjectItem
IndexedProjectListResponse
```

기존 `POST /v1/documents/ingest`가 사용하는 `IngestResponse` Python 모델은
유지한다. 새 Endpoint 경로 이름과 기존 Python 모델 이름이 같아 보이지만,
프로젝트 목록 응답에는 별도 모델을 사용하므로 기존 인제스트 응답과 충돌하지
않는다.

### 4.2 PostgreSQL Project Registry

`backend/project_store.py`에 다음 작업을 반영했다.

- `projects` 테이블 목록 조회
- `project_name ASC`, 동일 이름은 `project_id ASC` 정렬
- `index_completed_at TIMESTAMPTZ` nullable 컬럼 추가
- 문서 인덱싱 또는 Upload 인덱싱 성공 시 완료 시각 기록
- PostgreSQL 미설정·접속 실패를 `ProjectStoreError`로 전달

VectorDB point 목록을 조회하여 프로젝트 목록을 만들지 않는다. Project Registry는
PostgreSQL이 기준이고 VectorDB는 검색용 파생 데이터 저장소로 유지한다.

### 4.3 공개 인덱스 상태

```text
not_indexed
queued
indexing
ready
partially_ready
failed
stale
```

현재 Upload 내부 상태인 `completed`는 공개 API에서 `ready`로 변환한다.

### 4.4 활동 기록

`GET /v1/IngestResponse`를 Frontend 공개 Endpoint 모니터링 목록에 추가했다.

기록 항목:

- 요청 수신 여부
- 응답 반환 여부
- 마지막 HTTP status
- 마지막 성공 시각
- 마지막 응답시간
- 요청·성공·오류 횟수

---

## 5. 오류 상태별 처리 방법

### 5.1 정상 빈 목록

```http
200 OK
```

```json
{
  "schema_version": "1.0",
  "request_id": "req_01...",
  "projects": [],
  "total": 0,
  "generated_at": "2026-07-24T03:20:00Z"
}
```

Frontend 처리:

- 오류로 표시하지 않는다.
- `Backend에 등록된 프로젝트가 없습니다.`라고 표시한다.
- 새로고침 버튼은 계속 제공한다.

### 5.2 PostgreSQL 미설정 또는 장애

```http
503 Service Unavailable
```

```json
{
  "schema_version": "1.0",
  "request_id": "req_01...",
  "detail": "프로젝트 목록을 불러올 수 없습니다.",
  "error": {
    "code": "PROJECT_REGISTRY_UNAVAILABLE",
    "message": "프로젝트 목록을 불러올 수 없습니다.",
    "retryable": true,
    "issues": []
  }
}
```

Backend 처리:

- DB 장애를 `projects: []` 정상 응답으로 바꾸지 않는다.
- `503`과 구체적인 오류 code를 반환한다.
- DB credential이나 내부 접속정보는 반환하지 않는다.

Frontend 처리:

- 마지막 정상 목록이 있으면 삭제하지 않는다.
- `프로젝트 목록 서버를 사용할 수 없습니다.`라고 표시한다.
- 응답의 `request_id`를 장애 문의용으로 남긴다.
- `목록 새로고침`으로 재시도할 수 있게 한다.

### 5.3 Timeout 또는 Network 연결 실패

Frontend 처리:

- Backend에서 HTTP 응답을 받지 못한 통신 장애로 표시한다.
- 빈 프로젝트 목록으로 처리하지 않는다.
- 기존 정상 목록과 마지막 정상 조회 시각을 유지한다.
- 새로고침 버튼을 제공한다.
- 짧은 시간에 무제한 자동 재시도를 반복하지 않는다.

### 5.4 `403 Forbidden`

Frontend Client 연결 제어 또는 AllowList에 의해 거부된 상태다.

Frontend 처리:

- `관리자에 의해 Backend 연결이 비활성화되었습니다.`라고 안내한다.
- 일반 Network timeout과 구분한다.
- 자동 반복 호출을 중단하고 관리자 확인을 유도한다.

### 5.5 `422 Validation Error`

이 Endpoint는 GET이며 Request body가 없으므로 정상 구현에서는 일반적으로
`422`가 발생하지 않는다.

Frontend 처리:

- 잘못된 URL, Query 또는 계약 불일치로 취급한다.
- 요청 형식과 API 버전을 점검한다.
- 같은 요청을 자동 반복하지 않는다.

### 5.6 잘못된 JSON 또는 알 수 없는 Schema

Frontend 처리:

- 기존 정상 목록을 유지한다.
- `Backend 응답 형식이 현재 Extension과 호환되지 않습니다.`라고 표시한다.
- `schema_version`과 `request_id`를 로그에 기록한다.

### 5.7 인덱스 상태

`index_status`는 HTTP 장애가 아니라 프로젝트별 상태다.

| 상태 | Frontend 표시 |
|---|---|
| `not_indexed` | 인덱싱되지 않음 |
| `queued` | 인덱싱 대기 중 |
| `indexing` | 인덱싱 중 |
| `ready` | 사용 가능 |
| `partially_ready` | 일부 사용 가능 |
| `failed` | 인덱싱 실패 |
| `stale` | 갱신 필요 |

---

## 6. 관리자 페이지

관리자 페이지의 `API Endpoint` 영역에 다음 항목을 추가했다.

```text
GET /v1/IngestResponse — 인덱싱 프로젝트 목록
```

이번에 연결한 요청·응답 여부와 응답시간 확인은 실제 Backend 활동 데이터에
연결된다.

다만 관리자 페이지 전체 기능을 이번 작업에서 완성한 것은 아니다. VectorDB
재임베딩 버튼 등 화면 정의서용으로 만들어 둔 다른 영역은 계속 Mock일 수 있다.
이번 완료 범위는 `/v1/IngestResponse` Endpoint 모니터링 항목에 한정한다.

---

## 7. Frontend 팀 구현 항목

Frontend 팀은 VS Code Extension에 다음 기능을 구현한다.

1. Extension 최초 활성화 시 `GET /v1/IngestResponse` 호출
2. Sidebar `목록 새로고침`에서 동일 함수 재호출
3. 현재 Workspace 이름과 로컬 Git HEAD SHA 조회
4. Git remote를 `organization/repository` 형태로 정규화해 `project_id` 매칭
5. remote가 없을 때 프로젝트명을 보조 기준으로 사용
6. 전체 Git SHA 비교
7. 로컬 dirty 상태 별도 표시
8. `503`, timeout, `403`, 빈 목록, Schema 오류 구분
9. 실패 시 기존 정상 목록과 마지막 정상 조회 시각 유지

Backend의 다른 프로젝트는 `버전 불일치`가 아니라 `비교 대상 아님`으로
표시한다. 동명 프로젝트가 여러 개면 프로젝트명만으로 자동 매칭하지 않는다.

---

## 8. 이번 작업에서 제외한 항목

다음 기능은 관련성이 있지만 이번 구현 범위에 포함하지 않았다.

- `인덱싱 진행중 표시 (Backend)` 전체 화면
- 관리자의 `인덱스 갱신` 실행
- GitHub Actions push 자동 감지
- GitHub Organization Webhook
- 자동 재임베딩과 갱신 경고 생성
- Git이 아닌 프로젝트의 Manifest fingerprint 비교
- 프로젝트 목록 pagination·검색
- VS Code Extension Source 구현

GitHub main branch push 감지, 자동 최신 코드 수집, VectorDB 재임베딩과
Frontend 갱신 경고는 후속 자동화 작업으로 분리한다.

---

## 9. 변경 파일

| 파일 | 작업 |
|---|---|
| `backend/schemas.py` | 프로젝트 목록 공개 응답 모델 |
| `backend/project_store.py` | PostgreSQL 목록 조회·완료 시각 |
| `backend/app.py` | GET Route·상태 변환·오류·활동 기록 |
| `admin/src/main.ts` | 관리자 Endpoint 상태 항목 |
| `verify_ingest_response.py` | 목록·빈 목록·DB 장애 계약 검증 |
| `verify_api_contract.py` | OpenAPI 경로·Schema·해시 검증 |
| `API 통신 규약 V1 동결안.md` | 신규 Endpoint 계약 |
| `Frontend API 규약.md` | Frontend 구현 책임과 비교 규칙 |
| `docs/API_CONTRACT_V1.md` | 운영 계약과 OpenAPI 해시 |
| `IngestResponse 프로젝트 목록 조회 작업 계획.md` | Backend 완료·Frontend 인계 상태 |
| `README.md` | 작업 결과 보고 |

---

## 10. 검증 결과

| 검증 | 결과 |
|---|---|
| Python `compileall` | 통과 |
| `verify_ingest_response.py` | 통과 |
| `verify_api_contract.py` | 통과 |
| 관리자 TypeScript·Vite build | 통과 |
| 임시 HTTP 응답 Header와 `503` 오류 형식 | 통과 |

OpenAPI SHA-256:

```text
07c95ebc93d9a29d3e29b072e9bef71a4ed647b02ccb77a0b81865f8b252eeed
```

임시 HTTP 서버는 `127.0.0.1:8011`에서 검증 후 종료했다. 검증 당시 8000번
서버는 실행 중이 아니었다.

---

## 11. 실행 환경 주의

Docker Compose 환경에서는 PostgreSQL 접속정보와 비밀번호 파일을 Backend
컨테이너에 주입한다.

Windows에서 `python main.py`로 직접 실행할 때는 로컬 `.env`에 다음 설정이
필요하다.

```env
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=vision
POSTGRES_USER=vision
POSTGRES_PASSWORD=<실제 PostgreSQL 비밀번호>
```

현재 PostgreSQL 설정이 없으면 `/v1/IngestResponse`는 정상 빈 목록이 아니라
의도적으로 `503 PROJECT_REGISTRY_UNAVAILABLE`을 반환한다.

---

## 12. 요구사항명 정리

```text
/v1/IngestResponse
인덱싱 버전 비교 (Frontend)
인덱싱 버전 비교 (Backend)
오류 상태 표시
관리자 페이지 화면 설계서 Mock 및 Endpoint 모니터링 연결
```
 관련성은 있지만 이번 작업 계획의 직접 구현 대상은 아닙니다.