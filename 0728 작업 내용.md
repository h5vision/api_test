# 0728 작업 내용

> 문서 성격: 2026-07-28 BackendAPI 개발 및 운영 구조 개선 결과 보고서  
> 작업일: 2026-07-28  
> 작업 범위: FastAPI · Chat/RAG 계약 · AI Provider · Frontend Client 관리 · Redis Worker · Docker Compose 자동 증설 · 관리자 페이지 · 팀 전달 문서  
> 범위 제외: VS Code Extension Source 직접 구현

---

## 1. 오늘 작업 한눈에 보기

오늘 작업의 핵심은 기존 단일 FastAPI 서버를 다음과 같은 운영 가능한 구조로
확장한 것이다.

```text
VS Code Extension
        │
        │ /v1/chat · 프로젝트/인덱스 조회
        ▼
Cloudflare / Traefik
        │
        ├─ FastAPI API 인스턴스 1~6개
        │       ├─ PostgreSQL
        │       ├─ Qdrant
        │       ├─ Redis 공유 상태/작업 큐
        │       └─ AI Model Router
        │
        └─ Index Worker 인스턴스 1~4개
                └─ Repository Chunking · Embedding · Vector 저장
```

| 구분 | 오늘 작업 결과 | 상태 |
|---|---|---|
| Frontend Chat 계약 | 실제 Extension 요청 구조에 맞게 `/v1/chat` 보완 | 완료 |
| Chat Context | Frontend가 보내는 문자열 Context와 기존 배열 Context 모두 수신 | 완료 |
| AI Model | Local·외부 서버·Cloud Provider 모델 자동 탐색 및 선택 구조 보완 | 완료 |
| Groq/AWS 모델 | 관리자 Playground 및 Frontend Chat 경로에 연결 | 완료 |
| 통신 상태 | Frontend ↔ FastAPI ↔ AI Server 요청·응답·오류 로그 제공 | 완료 |
| Frontend Client | 첫 Chat 자동 등록과 관리자 T/F 차단 구현 | 완료 |
| 다중 API | Traefik이 여러 FastAPI 인스턴스로 요청 분산 | 완료 |
| 인덱싱 실행 | FastAPI Background Task를 Redis 기반 별도 Worker로 분리 | 완료 |
| 중복 작업 방지 | Redis, Consumer Group, PostgreSQL 제약을 이용한 다중 방어 | 완료 |
| 자동 증설 | 요청량·CPU·메모리·대기열에 따라 API/Worker 수 자동 조정 | 완료 |
| 관리자 페이지 | Client, 모델, VectorDB, 통신 상태, 인덱싱 상태를 한 화면에서 관리 | 완료 |
| 문서화 | Frontend·외부 AI Server·Client 등록·Autoscaling 가이드 작성 | 완료 |

Frontend 팀이 관리하는 다음 Source는 분석 목적으로만 확인했고 직접 수정하지
않았다.

```text
C:\Users\PC2412\Documents\HancomAI5\vision-frontend
```

---

## 2. `/v1/chat` Frontend 계약 보완

### 2.1 작업 목적

Frontend Extension이 실제로 보내는 요청을 Backend가 별도의 변환 과정 없이
받을 수 있게 계약을 맞췄다.

필수 입력값은 다음 세 개다.

| 필드 | 용도 |
|---|---|
| `project_id` | RAG 검색 범위를 결정하는 프로젝트 식별자 |
| `message` | 사용자의 질문 |
| `session_id` | 사용자별 대화 문맥을 분리하는 대화 세션 식별자 |

다음 값은 선택 입력값이다.

| 필드 | 기본 동작 |
|---|---|
| `history` | 생략하거나 `[]`로 보내면 새 대화로 처리 |
| `top_k` | 생략하면 Backend 기본 검색 개수 사용 |
| `context` | 생략 또는 `null`이면 빈 문자열로 처리 |
| `model_id` | 생략하면 관리자가 지정한 기본 모델 사용 |
| `client_request_id` | 생략하면 Backend가 Request ID 생성 |
| `stream` | 현재 계약에서는 생략하거나 `false` 사용 |

### 2.2 Context 호환

Frontend가 현재 파일 내용이나 선택한 코드 등을 문자열로 전달할 수 있도록
다음 형식을 지원한다.

```json
{
  "project_id": "Vision",
  "message": "이 문서를 요약해줘",
  "session_id": "9efda536-b502-49e4-926d-53343a428df0",
  "context": "파일: README.md\n\n프로젝트 설명 내용...",
  "history": [],
  "top_k": 3
}
```

기존 객체 배열 Context도 하위 호환 목적으로 유지한다.

- 문자열 Context 최대 크기: 5MB
- 배열 Context 최대 항목 수: 100개
- `context: null`은 빈 Context로 정규화

### 2.3 Chat 처리 흐름

```text
1. Frontend가 /v1/chat 요청
2. project_id 기준으로 Qdrant RAG 검색
3. PostgreSQL에서 문서·Chunk Metadata 확인
4. 검색 결과와 Frontend Context를 Prompt로 조립
5. 선택된 AI Model에 추론 요청
6. answer, source, metadata를 Frontend에 반환
```

Frontend는 응답의 HTTP 상태와 `Content-Type`을 먼저 확인한 뒤 JSON을
파싱해야 한다. HTML 502 응답을 JSON으로 파싱해 발생하던
`Unexpected token '<'` 오류를 구분하기 위한 규칙이다.

---

## 3. AI Model과 Provider 관리

### 3.1 지원 대상

다음 세 가지 배포 형태를 하나의 Model Router에서 사용할 수 있도록 보완했다.

| 형태 | 예시 | 등록 방식 |
|---|---|---|
| Local AI Server | 같은 PC의 Ollama | Host/IP와 Port 자동 탐색 |
| Remote AI Server | BackendAI 또는 별도 Ollama 서버 | Base URL 등록 후 모델 조회 |
| Cloud Provider | Groq, NVIDIA, AWS/OpenAI 호환 API | Base URL과 API Key 등록 |

### 3.2 자동 모델 탐색

AI Server가 실행 중이면 고정 모델명을 `.env`에 반드시 적지 않아도 다음
Endpoint를 이용해 모델 목록을 탐색할 수 있다.

- Ollama 호환: `/api/tags`
- OpenAI 호환: `/models`
- Backend 공개 모델 목록: `GET /v1/models`

관리자는 Provider의 Base URL, API Key, Protocol을 CRUD 방식으로 등록하고
모델 검색을 실행할 수 있다.

주요 관리자 API:

```text
GET    /v1/admin/ai-providers
POST   /v1/admin/ai-providers
PUT    /v1/admin/ai-providers/{provider_id}
DELETE /v1/admin/ai-providers/{provider_id}
POST   /v1/admin/ai-providers/{provider_id}/discover
POST   /v1/admin/ai-providers/scan-ollama
```

API Key는 관리자 응답이나 Frontend에 다시 노출하지 않고 암호화된 Provider
설정 또는 환경변수에서만 사용한다.

### 3.3 관리자 기본 모델

관리자 페이지에서 다음 항목을 구분해 표시하고 선택할 수 있도록 했다.

- 실제 모델명
- 공개 `model_id`
- Provider
- Cloud / Local / Remote Server
- 사용 가능 여부
- 기본 모델 여부

Frontend가 `model_id`를 보내지 않으면 관리자가 지정한 기본 모델을 사용한다.

---

## 4. Frontend ↔ FastAPI ↔ AI Server 통신 상태

### 4.1 추가 목적

Frontend에서 Chat 오류가 발생했을 때 어느 구간에서 실패했는지 관리자 페이지가
구분할 수 있도록 통신 상태와 이벤트를 저장한다.

```text
Frontend 요청 수신
       ↓
FastAPI RAG 처리
       ↓
AI Server 요청
       ↓
AI Server 응답
       ↓
Frontend 응답 전달
```

### 4.2 확인 가능한 내용

- Frontend 마지막 요청 시간
- Endpoint별 요청 수신 여부
- Endpoint별 응답 성공 여부
- HTTP 상태 코드
- 요청 처리 시간
- Request ID
- AI Provider와 실제 사용 모델
- FastAPI → AI Server 요청 상태
- AI Server → FastAPI 응답 상태
- 오류 종류와 발생 구간

관리자 API:

```text
GET  /v1/admin/connectivity
GET  /v1/admin/api-activity
GET  /v1/admin/communication-logs
POST /v1/admin/ai-probe
```

관리자 화면의 `systemStatus.ts`에서 이 정보를 주기적으로 조회한다.

---

## 5. Frontend Client 자동 등록과 연결 제어

### 5.1 변경 전 문제

기존 구조는 관리자가 Frontend IP와 Port를 미리 등록하고 Backend가 VS Code
Client의 `8888` Port로 역방향 연결을 시도했다.

하지만 VS Code Extension은 일반적으로 서버 역할을 하지 않고 Backend API에
요청을 보내는 Client이므로 역방향 TCP 확인은 실제 연결 가능 여부를 정확히
나타내지 못한다.

### 5.2 변경 후 동작

미등록 Extension이 처음 `/v1/chat`을 보내면 다음 순서로 처리한다.

```text
1. Backend가 미등록 Client 확인
2. PostgreSQL에 Client를 enabled=true로 자동 등록
3. 최초 Chat 요청을 중단하지 않고 그대로 처리
4. 응답 Header로 X-Client-ID 반환
5. 관리자는 관리자 페이지에서 Client를 확인
6. enabled=false로 변경하면 다음 요청부터 403 차단
```

권장 Header:

```http
X-Client-Type: vscode-extension
X-Client-Instance-ID: Extension 설치 단위 UUID
X-Client-ID: Backend가 이전 응답에서 발급한 ID
```

첫 요청에는 `X-Client-ID`가 없어도 된다. 응답에서 받은 뒤 VS Code
`globalState`에 보관해 이후 요청에 사용한다.

### 5.3 IP 사용 원칙

IP는 Client의 인증 ID로 사용하지 않고 접속 기록과 장애 진단용으로만 사용한다.

그 이유는 다음과 같다.

- 같은 회사 또는 가정의 여러 사용자가 동일 NAT IP를 사용할 수 있음
- Cloudflare Tunnel을 통과하면 공인 IP 또는 Proxy IP로 보일 수 있음
- Docker Desktop을 통과하면 `172.30.0.1` 같은 Gateway 주소로 변환될 수 있음

따라서 실제 Client 구분은 `X-Client-Instance-ID`와 `X-Client-ID`를 사용한다.

### 5.4 실제 검증 결과

| 검증 항목 | 결과 |
|---|---|
| 미등록 Client 첫 Chat | HTTP 200 |
| 자동 Client 등록 | 성공 |
| `X-Client-ID` 반환 | 성공 |
| `X-Client-Auto-Registered: true` | 성공 |
| 관리자 `enabled=false` 적용 | 성공 |
| 차단 후 같은 Client 재요청 | HTTP 403 |

---

## 6. 다중 FastAPI 인스턴스 상태 일관성

API 컨테이너가 여러 개 실행될 때 각 프로세스의 메모리 값이 달라지는 문제를
방지하기 위해 중요한 상태를 공용 저장소 기준으로 변경했다.

| 상태 | 공용 저장소 |
|---|---|
| Frontend Client 등록·허용·차단 | PostgreSQL |
| AI Model 사용 허용 여부 | PostgreSQL |
| BackendAI/Groq Runtime 설정 | PostgreSQL |
| 요청량과 활성 요청 수 | Redis |
| 인덱싱 대기·처리 작업 | Redis Stream |
| 프로젝트·Snapshot·Job 상태 | PostgreSQL |
| Embedding Vector | Qdrant |

Frontend 차단과 Model 사용 허용 여부는 각 API 프로세스의 오래된 Cache를
신뢰하지 않고 PostgreSQL을 다시 확인한다. 따라서 관리자가 값을 변경하면
여러 API 인스턴스에 동일하게 적용된다.

AI Server 상태 확인용 짧은 Cache는 접근 권한 판정이 아닌 외부 Server Probe
부하 감소 목적으로만 유지한다.

---

## 7. Redis 기반 인덱싱 Worker 분리

### 7.1 변경 목적

기존 FastAPI `BackgroundTasks` 방식은 API 컨테이너가 재시작되면 작업이
사라질 수 있고, API 인스턴스를 여러 개 실행할 때 동일 작업의 중복 실행을
통제하기 어려웠다.

이를 다음 구조로 변경했다.

```text
FastAPI
  │
  │ Job 생성
  ▼
PostgreSQL Job Registry
  │
  │ Task enqueue
  ▼
Redis Stream
  │
  ├─ Worker 1
  ├─ Worker 2
  └─ Worker N
        │
        ├─ Repository Snapshot
        ├─ Chunking
        ├─ BGE-M3 Embedding
        ├─ PostgreSQL Chunk/문서 저장
        └─ Qdrant Vector 저장
```

Worker가 처리하는 작업:

- Repository 인덱싱
- 중단된 Repository 인덱싱 재개
- Offline Embedding 결과 Import
- Frontend 분할 Upload 처리

### 7.2 장애 회수

Worker는 작업 중 lease를 주기적으로 갱신한다. Worker가 비정상 종료되어 lease가
만료되면 다른 Worker가 Redis `XAUTOCLAIM`을 이용해 작업을 회수한다.

Redis 전송 전에 PostgreSQL Job만 생성되고 Queue 전송이 실패한 경우에는 해당
Job을 `failed/queue_unavailable`로 변경한다. 따라서 실제 Queue에 없는 Job이
프로젝트를 계속 잠그지 않는다.

---

## 8. 작업 중복 방지

동일 인덱싱 작업이 여러 API 또는 여러 Worker에서 동시에 실행되지 않도록
다음 세 단계에서 차단한다.

### 8.1 Redis Dedupe Key

API가 동일 Task를 Queue에 넣을 때 Redis `SET NX`로 첫 요청만 등록한다.

### 8.2 Redis Consumer Group

한 Stream 메시지는 Consumer Group 안에서 한 Worker에만 전달된다.

### 8.3 PostgreSQL Unique Index

Repository Source 하나에 완료되지 않은 활성 Job이 둘 이상 생기지 않도록
Partial Unique Index를 적용했다.

Worker가 작업은 완료했지만 Redis ACK에 실패해 메시지가 다시 전달되는 경우에도
PostgreSQL Job 또는 Upload 상태가 이미 `completed`이면 실제 인덱싱을 다시
실행하지 않고 ACK만 처리한다.

검증 결과:

```text
첫 번째 enqueue  → true
같은 dedupe 요청 → false
Dead Task         → 0
```

---

## 9. 부하 기준 Docker Compose 자동 증설

### 9.1 증설 기준

Windows 11 + PowerShell + WSL2 Docker 환경에서 Host Autoscaler가 30초마다
다음 정보를 확인한다.

| 기준 | 기본 임계값 |
|---|---:|
| API 요청량 | API 인스턴스당 분당 120건 |
| 활성 요청 | API 인스턴스당 20건 |
| API 평균 CPU | 70% |
| API 평균 메모리 | 80% |
| 인덱싱 작업량 | Worker당 대기+처리 2건 |

### 9.2 인스턴스 범위

| 서비스 | 최소 | 최대 |
|---|---:|---:|
| FastAPI | 1 | 6 |
| Index Worker | 1 | 4 |

API는 요청량, 활성 요청, CPU, 메모리 중 하나라도 기준을 초과하면 한 개씩
증가한다. Worker 수는 Redis의 대기 작업과 처리 작업 개수를 기준으로 계산한다.

불필요한 증설·축소 반복을 방지하기 위해 다음 조건을 사용한다.

- 증설·축소 후 90초 Cooldown
- 5회 연속 저부하 확인 후 Scale-down
- Autoscaler 자체는 Windows Named Mutex로 중복 실행 방지

### 9.3 운영 명령

```powershell
# 현재 상태로 증설 판단만 확인
.\tools\autoscale_compose.ps1 -Once -DryRun

# 숨김 Process로 실행
.\tools\start_autoscaler.ps1

# 실행 로그 확인
Get-Content .\data\autoscaler.log -Wait

# 종료
.\tools\stop_autoscaler.ps1
```

### 9.4 실제 검증 결과

- FastAPI 1개 → 2개 증설 확인
- Traefik을 통한 두 API 인스턴스 요청 분산 확인
- Worker 1개 → 2개 증설 확인
- Redis 작업을 Worker 두 개가 나누어 처리하는 것 확인
- 저부하 상태에서 API 1개, Worker 1개로 유지
- Autoscaler 중복 시작 차단 확인

현재 확인 시점:

```text
FastAPI 인스턴스 : 1
Worker 인스턴스  : 1
Queue            : 0
Processing       : 0
Dead Task        : 0
```

---

## 10. 관리자 페이지 보완

관리자 페이지에서 다음 내용을 확인하거나 조작할 수 있도록 구성했다.

### 10.1 Frontend Client

- 자동 등록 Client와 관리자 수동 등록 Client 구분
- `AUTO` / `ADMIN` 표시
- Client Instance ID 표시
- 최근 요청 시간 표시
- Client T/F 연결 차단
- 여러 Frontend Client CRUD

### 10.2 AI Model

- 실제 선택 모델명 표시
- Provider 표시
- Local / Remote Server / Cloud 구분
- 사용 가능 여부 확인
- 관리자 기본 모델 지정
- 모델 자동 탐색
- Playground 실제 질문 전송

### 10.3 VectorDB와 인덱싱

- Qdrant 연결 상태
- Embedding Model과 Index Version
- Repository Source
- 인덱싱 진행 상태
- Queue 및 처리 중 작업
- 전체 재임베딩 실행

### 10.4 통신 상태

- Frontend ↔ FastAPI
- FastAPI ↔ AI Server
- Endpoint별 요청/응답
- 응답시간
- 통신 이벤트와 오류

관리자 UI는 Tailwind/Vite Build 후 Docker의 `admin_dist` Volume에 다시
배포했다.

---

## 11. 주요 API 정리

### Frontend 공개 API

| Method | Endpoint | 용도 |
|---|---|---|
| `GET` | `/v1/health` | Backend 상태 확인 |
| `GET` | `/v1/models` | 선택 가능한 AI 모델 목록 |
| `GET` | `/v1/IngestResponse` | 인덱싱 프로젝트와 Git 버전 목록 |
| `GET` | `/v1/repositories` | Backend Repository Source 목록 |
| `GET` | `/v1/repositories/{source_id}/tree` | Git/Local Repository 폴더 구조 |
| `GET` | `/v1/projects/{project_id}/tree` | 활성 DB Snapshot 폴더 구조 |
| `GET` | `/v1/indexing-jobs` | 인덱싱 진행 상태 |
| `POST` | `/v1/client-heartbeat` | Frontend 최근 연결 상태 |
| `POST` | `/v1/chat` | RAG 검색 및 AI 질의응답 |

### 관리자 내부 API

| Method | Endpoint | 용도 |
|---|---|---|
| `GET` | `/v1/admin/connectivity` | Frontend·AI Server 상태 |
| `GET` | `/v1/admin/api-activity` | Endpoint별 요청·응답 상태 |
| `GET` | `/v1/admin/communication-logs` | 구간별 통신 이벤트 |
| `GET` | `/v1/admin/runtime-metrics` | Redis 요청량·Queue 계측 |
| `GET/POST` | `/v1/admin/frontend-clients` | Client 목록·등록 |
| `PUT/DELETE` | `/v1/admin/frontend-clients/{client_id}` | Client 수정·삭제 |
| `GET/POST` | `/v1/admin/ai-providers` | AI Provider 목록·등록 |
| `POST` | `/v1/admin/ai-providers/{provider_id}/discover` | 모델 자동 탐색 |

---

## 12. 환경변수 및 Compose 변경

Redis 공유 상태와 Queue 실행을 위해 다음 설정을 추가했다.

```dotenv
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=운영용-비밀번호
REDIS_DB=0
TASK_QUEUE_NAME=vision:tasks:indexing
TRUSTED_PROXY_HOSTS=172.30.0.0/24
```

Python 의존성:

```text
redis>=5.2,<7
```

Compose에는 별도 `worker` 서비스가 추가되었다. Worker는 API와 같은 Source 및
Python 의존성을 사용하지만 HTTP Server를 실행하지 않고 Redis 작업만 처리한다.

---

## 13. 검증 결과

| 검증 항목 | 결과 |
|---|---|
| Python 전체 Compile | 성공 |
| Backend App/Worker Import | 성공 |
| OpenAPI v1 계약 Hash | 유지 |
| Chat 필수값 | `project_id`, `message`, `session_id` 유지 |
| Repository Tree 계약 | 성공 |
| Repository Resume 계약 | 성공 |
| `/v1/IngestResponse` 계약 | 성공 |
| 관리자 TypeScript 검사 | 성공 |
| 관리자 Vite Build | 성공 |
| 로컬 API `/v1/health` | HTTP 200 |
| 공개 Cloudflare API `/v1/health` | HTTP 200 |
| 관리자 페이지 | HTTP 200 |
| 첫 Chat 자동 등록 | 성공 |
| 관리자 Client 차단 | HTTP 403 확인 |
| Redis 동일 Task 중복 등록 | 차단 확인 |
| API 다중 인스턴스 분산 | 확인 |
| Worker 다중 인스턴스 분산 | 확인 |

API v1 공개 Schema Hash:

```text
61085a39c88bd48e1c5e8ada5b70bad1760801b9d473cd6247933400c336d272
```

기존 공개 Chat 계약의 필수값을 깨지 않고 운영 기능을 추가했다.

---

## 14. 주요 작업 파일

### Backend

```text
backend/app.py
backend/schemas.py
backend/ai_providers.py
backend/connectivity.py
backend/distributed.py
backend/frontend_clients.py
backend/generation.py
backend/model_access.py
backend/repository_indexer.py
backend/repository_store.py
backend/runtime_config.py
backend/runtime_services.py
backend/uploads.py
backend/worker.py
```

### 관리자 페이지

```text
admin/src/main.ts
admin/src/playground.ts
admin/src/systemStatus.ts
admin/src/styles.css
```

### 운영

```text
compose.yaml
requirements.txt
tools/autoscale_compose.ps1
tools/start_autoscaler.ps1
tools/stop_autoscaler.ps1
```

### 문서

```text
docs/API_CONTRACT_V1.md
docs/API_V1_TEAM_BRIEFING_KO.md
docs/FRONTEND_AI_ENDPOINT_HANDOFF_KO.md
docs/EXTERNAL_AI_SERVER_API_GUIDE_KO.md
docs/FRONTEND_GIT_AND_INDEXED_PROJECT_SIDEBAR_API_GUIDE_KO.md
docs/CLIENT_AUTO_REGISTRATION_AND_AUTOSCALING_KO.md
```

---

## 15. 팀별 전달 사항

### Frontend 팀

1. Backend Base URL을 사용자가 설정할 수 있게 한다.
2. 설치 단위 UUID를 생성해 `X-Client-Instance-ID`로 전송한다.
3. 첫 Chat 응답의 `X-Client-ID`를 `globalState`에 저장한다.
4. Chat마다 별도 `session_id`를 사용한다.
5. `403`은 관리자 차단 상태로 사용자에게 표시한다.
6. HTTP 상태와 Content-Type 확인 후 JSON을 파싱한다.
7. `429`, `502`, `503`, `504`는 지수 Backoff 대상으로 처리한다.
8. 프로젝트·Repository Tree와 인덱싱 진행 API를 Sidebar에 연결한다.

### BackendAPI 팀

1. PostgreSQL, Qdrant, Redis 상태를 지속 확인한다.
2. Autoscaler 로그와 Dead Task를 모니터링한다.
3. Client T/F와 기본 모델을 관리자 페이지에서 운영한다.
4. API v1 공개 Schema 변경 시 계약 검증 Script를 먼저 통과시킨다.
5. Queue 장애 시 `queue_unavailable` Job을 확인하고 재시도한다.

### AI Server 팀

1. Ollama 또는 OpenAI 호환 모델 조회 API를 제공한다.
2. 실제 Chat Endpoint와 Model ID를 BackendAPI 팀에 전달한다.
3. Health와 모델 목록 응답을 안정적으로 유지한다.
4. Timeout, 최대 Context, 동시 요청 수를 BackendAPI 팀과 합의한다.

---

## 16. 후속 작업

- Frontend Extension에 `X-Client-Instance-ID` 영구 저장 구현
- Frontend Sidebar에 Repository/DB Snapshot Tree 연결
- Frontend Sidebar에 인덱싱 진행 상태 표시
- Windows 재부팅 후 Autoscaler 자동 시작을 위한 작업 스케줄러 등록 검토
- 운영 부하 시험 후 API·Worker 임계값 재조정
- Dead Task 관리자 화면과 재처리 기능 검토
- Redis, PostgreSQL, Qdrant Backup/복구 정책 수립
- Streaming 재연결 및 Non-stream Fallback 통합 시험

---

## 17. 최종 요약

오늘 작업으로 BackendAPI는 단순한 단일 FastAPI Chat Server에서 다음 기능을
가진 구조로 확장되었다.

1. Frontend 실제 요청 계약과 호환되는 RAG Chat
2. Local·Remote·Cloud AI Model 자동 탐색과 선택
3. Frontend Client 첫 Chat 자동 등록과 관리자 차단
4. PostgreSQL·Redis 기반 다중 API 상태 일관성
5. Redis Stream 기반 독립 인덱싱 Worker
6. 다중 Worker 작업 중복 방지와 장애 회수
7. 요청량·CPU·메모리·Queue 기반 Docker Compose 자동 증설
8. 관리자 페이지를 통한 통신·모델·Client·VectorDB 통합 운영

공개 API와 관리자 페이지는 실제 배포 경로에서 HTTP 200을 확인했으며,
Client 자동 등록·차단, Redis 중복 방지, API/Worker 증설과 분산 처리까지
검증했다.
